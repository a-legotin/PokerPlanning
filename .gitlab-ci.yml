image: microsoft/dotnet:latest


stages:
    - build

before_script:
    - "dotnet restore"

build:
    stage: build
    tags:
        - local
    script:
        - cd PokerPlanning/
        - "dotnet build"
